language: java
sudo: false
dist: trusty

jdk:
  - oraclejdk7
  - oraclejdk8

install:
  - travis_wait mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V

script:
  - travis_wait mvn test -B

notifications:
  email: false
